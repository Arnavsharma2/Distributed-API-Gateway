global:
  scrape_interval: 5s

scrape_configs:
  - job_name: gatekeeper
    dns_sd_configs:
      - names:
          - gateway
        type: A
        port: 9090

  - job_name: prometheus
    static_configs:
      - targets:
          - localhost:9090
